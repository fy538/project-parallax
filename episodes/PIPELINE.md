# Episode Pipeline State

> Strategic tracker for episode state + narrative. **State + dates** live in [`pipeline-state.json`](./pipeline-state.json) (machine-readable, single source of truth). **Operational status** (artifact presence, blockers, asset counts, render status, cost) lives in per-episode `<slug>/_status.md` dashboards — auto-generated by `tools/pipeline_validator.py --write-status`; don't hand-edit.
>
> The **At a glance** table below is auto-refreshed on every `./scripts/check-episode.sh <slug>` run. Hand-edit the drill-down prose sections for narrative — that's where strategic thinking belongs.

## At a glance

| Episode | State | Day | Target | Health |
|---|---|---|---|---|
| `prisoners-dilemma` | 🎬 RENDER READY | 8 | 2026-06-01 | ✗ never rendered |
| `silicon-trap` | 🎬 RENDER READY | 16 | 2026-05-25 | ⚠ stale manifest (4.5 d) · 🔴 21 zero-hit shots · ✗ never rendered |
| `blockades-leak` | 💭 INCUBATING | 60 | — | ⏸ awaiting promotion |

_Health column auto-refreshed by `python3 tools/pipeline_validator.py --update-tracker` (wired into `check-episode.sh` W9 follow-up). State + dates: edit `pipeline-state.json`._

## Session start (2 min)

Run this at the top of every work session before opening any episode files:

1. **Glance at the table above** — which episodes are blocked? Which target dates are slipping (negative "days to target")?
2. **Pick the episode you'll advance this session** — don't context-switch mid-session.
3. **Open its drill-down section below** for narrative context, then open its `_status.md` for the operational checklist + exact-command fixes for any health failures.
4. **Skill workflow:** the per-episode skill (script-draft, visual-spec, render-qa, etc.) reads `_status.md` + the episode folder; it doesn't parse this file directly.

If a new topic idea arrived since the last session, run `topic-viability` on it before adding it to `pipeline-state.json` — don't promote to INCUBATING without a gate verdict.

---

## 🎬 prisoners-dilemma — RENDER READY (day 8)

**Target:** 2026-06-01 (+15 days) · **Format:** Philosopher's Lens · **Status:** [`prisoners-dilemma/_status.md`](prisoners-dilemma/_status.md)

The launch candidate. Script is locked editorially (~893s / 14.9 min, 121 segments across 49 template data files). The asset-source workflow bypassed the canonical `source.py` flow entirely — 17 stills + 17 clips were generated via a sibling Recraft / Flux AI-gen pipeline that lives in `episodes/prisoners-dilemma/{video-prompts.md, chatgpt-prompts.md, chatgpt-prompts-v2.md, chatgpt-regen-prompts.md}` and produced files directly into `assets/{stills,clips}/`. No `asset-manifest.json` exists for the canonical pipeline to track against. The remaining work is the first end-to-end render, narration, and NLE polish.

**Next 3 actions:**
1. Re-render the manifest from current script if stale (none today, but check on next script edit).
2. First full-episode render: `cd remotion-templates && node scripts/render-episode.mjs --episode=prisoners-dilemma`.
3. Record narration → `episodes/prisoners-dilemma/assets/narration.wav` → re-run `generate_manifest.py --audio` to promote the manifest from `estimate` to `precise` mode.

---

## 🎬 silicon-trap — RENDER READY (day 16, **target slipping**)

**Target:** 2026-05-25 (+8 days) · **Format:** Wargamer · **Status:** [`silicon-trap/_status.md`](silicon-trap/_status.md)

Editorially production-ready: script v5 (`script-v5-production.md`, 99 segments, 13.5 min, estimate mode). Manifest was last regenerated May 11 — **stale by 4.5 days** relative to script v5 (May 16). The wall is **21 zero-hit FOOTAGE shots** from Pexels with no AI-gen content produced yet: `episodes/silicon-trap/ai-gen-briefs.md` was generated by `zerohit_fallback.py` and contains starter briefs (Recraft anchor + Flux style-ref suggestions per shot), but nothing has been actioned. Narration not recorded.

Critical path: action the 21 briefs to get assets, refresh the manifest, render, narrate. Tighter window than prisoners-dilemma.

**Next 3 actions:**
1. **Refresh manifest** to match script v5: `python3 tools/assembly/generate_manifest.py silicon-trap`.
2. **Action the 21 zero-hit briefs** in `episodes/silicon-trap/ai-gen-briefs.md` — generate each via Recraft (stills) or Flux 2 Pro on fal.ai (video clips); drop into `episodes/silicon-trap/assets/`.
3. **First full-episode render** with refreshed manifest + new assets.

---

## 💭 blockades-leak — INCUBATING (day 60, stalled)

**Target:** — (no commit date) · **Status:** [`blockades-leak/_status.md`](blockades-leak/_status.md)

Awaiting viability re-gate. The v1 research returned CONDITIONAL on low historical depth — the analogy structure was thin. Stalled 60 days; the artifact graveyard (research + angle-memo + script + visual-spec all present from the v1 pass) indicates the episode was further along before reverting to INCUBATING.

Decision pending: either dig into the historical-depth gap and re-research, or formally demote to BACKLOG.

**Next action:** Re-run `topic-viability` with the historical-depth-gap explicitly named in the input; advance to RESEARCHING (with new prompts) or demote.

---

## Lifecycle states

States flow left-to-right through the production pipeline (see [`project/PRODUCTION_PIPELINE.md`](../project/PRODUCTION_PIPELINE.md) for the full stage map). Source of truth for the names: [`pipeline-state.json`](./pipeline-state.json) `stateLifecycle` array.

| State | What it means | Entry gate | Exit gate |
|---|---|---|---|
| `INCUBATING` | Topic identified; not yet committed | Signal detected, ≥3-point signal test | `topic-viability` returns VIABLE |
| `VIABLE` | Cleared viability gate; ready to research | `topic-viability` VIABLE | Deep Research scheduled |
| `RESEARCHING` | Deep Research in progress | Research kicked off | All 3 passes complete |
| `RESEARCH READY` | Brief complete; ready to draft | `research-audit` passes | Angle picked + script-draft scheduled |
| `DRAFTING` | Script in progress | `script-draft` started | Script complete + `script-audit` passes |
| `RENDER READY` | Script + visual spec done; data files generated | `visual-spec` complete + manifest generated | All clips rendered + assembled |
| `IN POST` | Renders done; narration + NLE assembly | All clips rendered | Final master + thumbnail finalized |
| `PUBLISHED` | Live on YouTube | Master uploaded + scheduled | — |
| `RETROED` | `publish-retro` complete; `LEARNING_LOG.md` updated | 7-14 days post-publish | — |

### Failure states

When a gate rejects an episode, use one of these states instead of leaving it in the current forward state.

| State | What it means | Recovery action |
|---|---|---|
| `BLOCKED` | Cannot advance — external dependency unresolved | Update `notes` in `pipeline-state.json` with the blocker; revisit on next session |
| `REVISING` | Returned from a failed gate | Note the gate that failed in `notes`; re-run gate after fix |

**Common failure paths:**
- `research-audit` returns NEEDS MORE RESEARCH → revert to `RESEARCHING`
- `visual-concept` escalates a P1 gap back to script → revert to `DRAFTING`
- `source-feedback` flags a P1 hero visual as unavailable → stays `RENDER READY`, blocked on visual replacement decision

## Update protocol

**Starting a new episode:** Copy `episodes/EPISODE_TEMPLATE/` to `episodes/<slug>/` (or run `/new-episode <slug>`). Then add an entry to `episodes/pipeline-state.json` with `state: "INCUBATING"`, today's `stateEnteredAt`, and a `notes` field describing the signal that triggered promotion.

**When an episode changes state:**
1. **Update `pipeline-state.json`** — change the `state` field, set `stateEnteredAt` to today's date, update `notes` and `targetPublish` if applicable.
2. **Refresh the tracker**: `python3 tools/pipeline_validator.py --write-status <slug> --update-tracker`. This rewrites `<slug>/_status.md` AND updates the Health column row in PIPELINE.md.
3. **Hand-edit the drill-down section** above for narrative changes — the auto-refresh only touches the At-a-glance table, not the prose.
4. When an episode publishes, also update [`publish-order.json`](./publish-order.json) with the assigned episode number.

**When a gate fails:** Change `state` to `REVISING` or `BLOCKED` in `pipeline-state.json`, record the reason in `notes`, and run the refresh. Don't leave an episode in its current forward state with a silent blocker.

## File naming convention

**Canonical outputs** use the plain filename. **Draft/versioned outputs** use a version suffix. When a gate passes, rename the accepted version to the canonical name.

| Artifact | Canonical name | Versioned drafts |
|---|---|---|
| Research brief | `brief.md` | — (final combined; per-pass files are separate artifacts below) |
| Research passes | `research-pass1.md`, `research-pass2.md`, `research-pass3.md` | — (each is its own artifact; keep if materially different from brief.md) |
| Research bridge | `research-bridge-output.md` | — (single output per pass pair) |
| Viability check | `viability.md` | — (one per topic; rewrite in place if re-run) |
| Script | `script-production.md` | `script-v2-production.md`, `script-v3-production.md`, etc. |
| Angle memo | `angle-memo.md` | — (rewrite in place) |
| Persona eval | `persona-eval.md` | `persona-eval-v2.md`, etc. (keep prior versions if substantially different) |
| Script audit | `script-audit.md` | `script-audit-v2.md`, etc. |
| Visual concept | `visual-concept-audit.md` | `visual-concept-v2.md`, etc. |
| Review package | `review-package.md` | `review-package-v2.md`, etc. |
| Visual spec | `visual-spec.md` | — (regenerate from script; don't version) |
| Visual QA | `visual-qa.md` | — (regenerate per render; don't version) |
| Revision log | `REVISION_LOG.md` | — (append-only; never versioned) |
| Auto status | `_status.md` | — (regenerated by `pipeline_validator.py --write-status`; don't hand-edit) |

**Rule:** skills that read prior outputs (review-package reading script-audit, etc.) should always open the canonical filename, not a versioned file. If the canonical file is missing, that's a signal the gate hasn't been formally accepted yet.

---

## Backlog (not yet promoted to state)

Topics that have passed the signal-watch gate but are still pre-`INCUBATING`. See [`project/IDEAS.md`](../project/IDEAS.md) for the full topic pipeline including `Signal Detected` entries.
