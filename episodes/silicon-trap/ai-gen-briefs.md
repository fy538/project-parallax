# AI-Gen Briefs — `silicon-trap`

_Generated by `tools/asset-source/zerohit_fallback.py`._

**21 shots** in this episode returned zero stock hits or zero successful downloads. Each entry below is a starter brief for the AI-gen fallback pipeline (Recraft for stills, Flux 2 Pro on fal.ai for video). Edit the prompts before generating — they're heuristic seeds, not production-ready copy.

---

## beat1-tsmc-aerial (P1)

**Original search** (zero hits on stock platforms):
- `TSMC Arizona construction aerial drone`
- `semiconductor factory aerial desert`
- `large factory construction aerial`

**Shot-list notes**: Opening shot — needs to be cinematic. Desert landscape with massive industrial construction.

**Suggested anchor** (Recraft): `A2` — Industrial chokepoint
**Suggested style ref** (Flux): `r9` — see `tools/ai-video/style-references/r9_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
TSMC Arizona construction aerial drone. Opening shot — needs to be cinematic. Desert landscape with massive industrial construction.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat1-cleanroom (P1)

**Original search** (zero hits on stock platforms):
- `semiconductor cleanroom wafer handling`
- `chip factory cleanroom workers`
- `cleanroom manufacturing`

**Shot-list notes**: Macro footage of wafer handling. Slow, deliberate shots emphasizing precision.

**Suggested anchor** (Recraft): `A2` — Industrial chokepoint
**Suggested style ref** (Flux): `r2` — see `tools/ai-video/style-references/r2_*.png`
**Target tool**: Flux 2 Pro on fal.ai (video)
**Format**: 16:9 video, 5-8s

**Generation prompt** (seed from anchor + shot context — edit before running):

```
semiconductor cleanroom wafer handling. Macro footage of wafer handling. Slow, deliberate shots emphasizing precision.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat1-desert-housing (P2)

**Original search** (zero hits on stock platforms):
- `Arizona desert housing construction workers`
- `temporary worker housing desert`
- `Arizona suburban construction`

**Shot-list notes**: Contrast with high-tech cleanroom — mundane temporary housing in desert heat.

**Suggested anchor** (Recraft): `A2` — Industrial chokepoint
**Suggested style ref** (Flux): `r2` — see `tools/ai-video/style-references/r2_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
Arizona desert housing construction workers. Contrast with high-tech cleanroom — mundane temporary housing in desert heat.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat1-global-network (P3)

**Original search** (zero hits on stock platforms):
- `world map connections network global`
- `global supply chain visualization`

**Shot-list notes**: Ambient texture under narration about supply chain complexity.

**Suggested anchor** (Recraft): `A3` — Strategic plate
**Suggested style ref** (Flux): `r15` — see `tools/ai-video/style-references/r15_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
world map connections network global. Ambient texture under narration about supply chain complexity.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat2-fdr-1941 (P1)

**Original search** (zero hits on stock platforms):
- `FDR signing executive order 1941`
- `Roosevelt oil embargo Japan`
- `1941 US foreign policy`

**Shot-list notes**: Historical archival. Wikimedia Commons or Library of Congress preferred.

**Suggested anchor** (Recraft): `A4` — Archival document
**Suggested style ref** (Flux): `r6` — see `tools/ai-video/style-references/r6_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
FDR signing executive order 1941. Historical archival. Wikimedia Commons or Library of Congress preferred.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat2-jake-sullivan (P2)

**Original search** (zero hits on stock platforms):
- `Jake Sullivan national security advisor`
- `Jake Sullivan speaking podium`

**Shot-list notes**: Background under narration about Sullivan's 2022 speech.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r15` — see `tools/ai-video/style-references/r15_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
Jake Sullivan national security advisor. Background under narration about Sullivan's 2022 speech.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat2-chip-macro (P3)

**Original search** (zero hits on stock platforms):
- `semiconductor chip macro photography`
- `computer chip closeup circuit`
- `microprocessor macro`

**Shot-list notes**: Generic chip imagery for transitions.

**Suggested anchor** (Recraft): `A2` — Industrial chokepoint
**Suggested style ref** (Flux): `r15` — see `tools/ai-video/style-references/r15_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
semiconductor chip macro photography. Generic chip imagery for transitions.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat3-china-rail (P1)

**Original search** (zero hits on stock platforms):
- `China high speed rail`
- `Chinese bullet train`
- `CRH high speed train`

**Shot-list notes**: Part of the ballpoint pen contrast sequence. Needs to look impressive/modern.

**Suggested anchor** (Recraft): `A6` — Conceptual corridor
**Suggested style ref** (Flux): `r12` — see `tools/ai-video/style-references/r12_*.png`
**Target tool**: Flux 2 Pro on fal.ai (video)
**Format**: 16:9 video, 5-8s

**Generation prompt** (seed from anchor + shot context — edit before running):

```
China high speed rail. Part of the ballpoint pen contrast sequence. Needs to look impressive/modern.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat3-space-launch (P2)

**Original search** (zero hits on stock platforms):
- `China space launch rocket`
- `Chinese space program launch`
- `Shenzhou launch`

**Shot-list notes**: Part of the ballpoint pen contrast sequence.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r12` — see `tools/ai-video/style-references/r12_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
China space launch rocket. Part of the ballpoint pen contrast sequence.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat3-pen-tip (P1)

**Original search** (zero hits on stock platforms):
- `ballpoint pen tip macro extreme closeup`
- `pen tip macro metal`
- `ballpoint pen closeup`

**Shot-list notes**: The visual punchline — tiny pen tip after massive achievements. Needs to be high quality macro.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r15` — see `tools/ai-video/style-references/r15_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
ballpoint pen tip macro extreme closeup. The visual punchline — tiny pen tip after massive achievements. Needs to be high quality macro.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat3-shenzhen (P3)

**Original search** (zero hits on stock platforms):
- `Shenzhen skyline technology district`
- `Chinese city technology night`
- `Shenzhen aerial night`

**Shot-list notes**: Ambient establishing shot for China segment.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r12` — see `tools/ai-video/style-references/r12_*.png`
**Target tool**: Flux 2 Pro on fal.ai (video)
**Format**: 16:9 video, 5-8s

**Generation prompt** (seed from anchor + shot context — edit before running):

```
Shenzhen skyline technology district. Ambient establishing shot for China segment.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat3-ai-datacenter (P3)

**Original search** (zero hits on stock platforms):
- `artificial intelligence data center servers`
- `AI computing server room`
- `GPU server cluster`

**Shot-list notes**: Transition into DeepSeek section.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r9` — see `tools/ai-video/style-references/r9_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
artificial intelligence data center servers. Transition into DeepSeek section.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat3-deepseek (P2)

**Original search** (zero hits on stock platforms):
- `DeepSeek AI logo`
- `Chinese AI company office`
- `DeepSeek artificial intelligence`

**Shot-list notes**: DeepSeek R1/R2 discussion. Logo preferred; fallback to generic Chinese AI lab imagery.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r2` — see `tools/ai-video/style-references/r2_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
DeepSeek AI logo. DeepSeek R1/R2 discussion. Logo preferred; fallback to generic Chinese AI lab imagery.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat3-wafer-macro (P3)

**Original search** (zero hits on stock platforms):
- `semiconductor wafer macro`
- `silicon wafer close up`
- `chip wafer colorful`

**Shot-list notes**: Ambient closing texture for Beat 3.

**Suggested anchor** (Recraft): `A2` — Industrial chokepoint
**Suggested style ref** (Flux): `r2` — see `tools/ai-video/style-references/r2_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
semiconductor wafer macro. Ambient closing texture for Beat 3.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat4-world-divided (P3)

**Original search** (zero hits on stock platforms):
- `world map divided`
- `geopolitical divide concept`
- `global split east west`

**Shot-list notes**: Background for bifurcation discussion.

**Suggested anchor** (Recraft): `A3` — Strategic plate
**Suggested style ref** (Flux): `r7` — see `tools/ai-video/style-references/r7_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
world map divided. Background for bifurcation discussion.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat5-car-dashboard (P1)

**Original search** (zero hits on stock platforms):
- `car dashboard electronics digital`
- `modern car interior technology`
- `automotive electronics closeup`

**Shot-list notes**: Quick montage element — everyday devices with chips.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r3` — see `tools/ai-video/style-references/r3_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
car dashboard electronics digital. Quick montage element — everyday devices with chips.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat5-phone-circuit (P1)

**Original search** (zero hits on stock platforms):
- `smartphone circuit board inside`
- `phone motherboard components`
- `mobile phone internal electronics`

**Shot-list notes**: Quick montage element.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r15` — see `tools/ai-video/style-references/r15_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
smartphone circuit board inside. Quick montage element.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat5-hospital-mri (P1)

**Original search** (zero hits on stock platforms):
- `hospital MRI machine`
- `MRI scanner medical`
- `medical imaging equipment`

**Shot-list notes**: Quick montage element.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r15` — see `tools/ai-video/style-references/r15_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
hospital MRI machine. Quick montage element.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat5-data-center (P1)

**Original search** (zero hits on stock platforms):
- `data center server rack`
- `server room rows`
- `cloud computing infrastructure`

**Shot-list notes**: Quick montage element.

**Suggested anchor** (Recraft): `A1` — Imperial transition
**Suggested style ref** (Flux): `r15` — see `tools/ai-video/style-references/r15_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
data center server rack. Quick montage element.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat5-car-shortage (P2)

**Original search** (zero hits on stock platforms):
- `empty car dealership lot 2021`
- `auto factory shutdown chip shortage`
- `car shortage empty lot`

**Shot-list notes**: COVID chip shortage reference — empty lots, shut factories.

**Suggested anchor** (Recraft): `A2` — Industrial chokepoint
**Suggested style ref** (Flux): `r15` — see `tools/ai-video/style-references/r15_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
empty car dealership lot 2021. COVID chip shortage reference — empty lots, shut factories.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

## beat5-earth-night (P3)

**Original search** (zero hits on stock platforms):
- `world map night lights satellite`
- `earth from space night lights`
- `satellite view earth night`

**Shot-list notes**: Closing ambient shot — the connected world at risk.

**Suggested anchor** (Recraft): `A3` — Strategic plate
**Suggested style ref** (Flux): `r15` — see `tools/ai-video/style-references/r15_*.png`
**Target tool**: Recraft (still)
**Format**: 1920×1080 still

**Generation prompt** (seed from anchor + shot context — edit before running):

```
world map night lights satellite. Closing ambient shot — the connected world at risk.
```

**Post-gen**: route through `tools/brand-treatment/treat.py` with `--ramp standard` (or `conflict`/`editorial` per shot register).

---

# Appendix — Recraft Anchor Catalog (A1-A7)

Override the heuristic suggestion above when none of the matches fit.

### `A1` — Imperial transition
_Use for_: civilizational decline, fall of empire, succession crisis, post-imperial geography, Roman / Ottoman / Qing analogies

### `A2` — Industrial chokepoint
_Use for_: supply chain dependency, chip fab / fabrication interior, shipping lane / port, energy infrastructure, Silicon Trap-style chokepoint visuals

### `A3` — Strategic plate
_Use for_: war room / situation board, decision-maker tableau, central bank / policy committee, game-theoretic deliberation scene, executive negotiation

### `A4` — Archival document
_Use for_: declassified file / treaty draft, intelligence briefing pages, newspaper front page reveal, policy memo / executive order, footnote / source provenance moment

### `A5` — Solitary thinker
_Use for_: philosopher / strategist alone, Philosopher's Lens episode cold open, framework introduction beat, Nash / Schelling / Tetlock-style intellectual scene, audience-surrogate contemplation moment

### `A6` — Conceptual corridor
_Use for_: abstract concept as physical metaphor, trap / lock-in / one-way path, bifurcation / branching futures, sanctioned / sealed-off space, Silicon Trap / Prisoner's Dilemma metaphor visuals

### `A7` — Civic crowd
_Use for_: anonymous masses / populace, mobilization / public sentiment, legitimacy / consent of the governed, demographic structural force, post-event street scene

# Appendix — Flux Style Reference Catalog (r1-r15)

Full index (with locks and LUT) at `tools/ai-video/style-references/INDEX.md`.
