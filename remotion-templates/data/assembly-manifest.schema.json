{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "assembly-manifest.schema.json",
  "title": "Parallax Assembly Manifest",
  "description": "Maps every second of a Parallax episode to a visual element, bridging the two-column production script and the Remotion full-episode composition. Generated by tools/assembly/generate-manifest.py, consumed by src/templates/Episodes/FullEpisode.tsx.",
  "type": "object",
  "required": ["version", "episode", "fps", "narration", "segments"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "const": "1.0",
      "description": "Schema version for forward compatibility."
    },
    "episode": {
      "type": "string",
      "description": "Episode slug identifier, e.g. 'silicon-trap'."
    },
    "title": {
      "type": "string",
      "description": "Episode title for display, e.g. 'The Silicon Trap'."
    },
    "fps": {
      "type": "integer",
      "description": "Frame rate. Must match layout.fps in theme.ts (currently 30).",
      "default": 30
    },
    "totalDurationSec": {
      "type": "number",
      "description": "Total episode duration in seconds. Computed from narration length + padding."
    },
    "mode": {
      "type": "string",
      "enum": ["estimate", "precise"],
      "description": "How timing was derived. 'estimate' = from script hints and word count. 'precise' = from Whisper narration timestamps."
    },
    "pacing": {
      "type": "object",
      "description": "PACE: annotation summary emitted by generate_manifest.py. Records which pacing profiles were applied and any net timing delta.",
      "additionalProperties": false,
      "properties": {
        "multipliers": {
          "type": "object",
          "description": "Duration multipliers per pacing profile.",
          "additionalProperties": { "type": "number" }
        },
        "profilesUsed": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Pacing profiles referenced in the script."
        },
        "diffs": {
          "type": "array",
          "description": "Segment-level timing adjustments.",
          "items": { "type": "object" }
        },
        "netDeltaSec": {
          "type": "number",
          "description": "Net timing change in seconds from pacing adjustments."
        }
      }
    },
    "episodeColorEmphasis": {
      "type": "string",
      "enum": ["neutral", "soviet", "american-modernist", "chinese-state", "chinese-traditional", "japanese-showa"],
      "default": "neutral",
      "description": "Per-episode color emphasis for Remotion templates. Mirrors per-typography palette emphasis architecture in tools/recraft/recraft.py. Specifies which subset of the brand palette to foreground in chart fills, accent labels, highlight elements. Creates per-episode visual unity between AI-generated cover illustrations (Registers 2/3) and Remotion analytical inside-layout (Register 1). Consumed by theme.ts via getEpisodeColorEmphasis(). Falls back to 'neutral' if unset, preserving backward compatibility. See BRAND.md 'Per-Episode Color Emphasis' for editorial rationale."
    },
    "narration": {
      "type": "object",
      "description": "Narration audio track metadata.",
      "required": ["totalDurationSec"],
      "additionalProperties": false,
      "properties": {
        "audioFile": {
          "type": "string",
          "description": "Path to narration audio file, relative to the episode data directory. Null in estimate mode."
        },
        "totalDurationSec": {
          "type": "number",
          "description": "Total narration audio duration in seconds."
        },
        "words": {
          "type": "array",
          "description": "Word-level timestamps from WhisperX (phoneme-level alignment, ±50ms). Empty in estimate mode.",
          "items": {
            "type": "object",
            "required": ["word", "start", "end"],
            "additionalProperties": false,
            "properties": {
              "word": { "type": "string" },
              "start": { "type": "number", "description": "Start time in seconds." },
              "end": { "type": "number", "description": "End time in seconds." }
            }
          }
        }
      }
    },
    "beats": {
      "type": "array",
      "description": "Beat boundaries for structural reference. Not used for rendering — segments carry their own timing.",
      "items": {
        "type": "object",
        "required": ["id", "title", "startSec"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "e.g. 'beat1', 'opening', 'closing'" },
          "title": { "type": "string" },
          "startSec": { "type": "number" },
          "endSec": { "type": "number" }
        }
      }
    },
    "filmOverlay": {
      "type": "object",
      "description": "Episode-level film overlay config. Wraps all visual layers with cinematic texture effects (grain, vignette, light-leak, etc.).",
      "additionalProperties": false,
      "properties": {
        "preset": {
          "type": "string",
          "enum": ["clean", "documentary", "cinematic", "dramatic", "archival"],
          "description": "Named preset from FilmOverlayPresets.ts. If specified, effects and intensity are derived from the preset."
        },
        "effects": {
          "type": "array",
          "items": { "type": "string", "enum": ["grain", "vignette", "light-leak", "dust", "scratch", "flicker"] },
          "description": "Which effects to enable. Overrides preset if both specified."
        },
        "intensity": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Overall intensity (0-1). Overrides preset if both specified."
        }
      }
    },
    "lowerThird": {
      "type": "object",
      "description": "Episode-level lower-third config. When present, renders a LowerThird per beat with the specified label and timing.",
      "additionalProperties": false,
      "properties": {
        "label": {
          "type": "string",
          "description": "Label shown on all lower-thirds, e.g. '∴ PARALLAX · EP.01'."
        },
        "accentColor": {
          "type": "string",
          "description": "Accent color for the vertical line (default: gold #C4A747)."
        },
        "mode": {
          "type": "string",
          "enum": ["dark", "light"],
          "description": "Color mode override (default: dark)."
        },
        "enterDelaySec": {
          "type": "number",
          "default": 0.5,
          "description": "Seconds after beat start to show lower-third."
        },
        "displayDurationSec": {
          "type": "number",
          "default": 4,
          "description": "Seconds to display before hiding."
        }
      }
    },
    "musicBed": {
      "type": "object",
      "description": "Episode-level music bed definition (Layer 1). Continuous ambient music tracks with crossfade transitions at beat boundaries.",
      "additionalProperties": false,
      "properties": {
        "tracks": {
          "type": "array",
          "description": "Ordered list of music bed tracks. Tracks may overlap by their fade durations to create crossfade transitions.",
          "items": {
            "type": "object",
            "required": ["id", "file", "startSec", "endSec", "volume", "mood"],
            "additionalProperties": false,
            "properties": {
              "id": {
                "type": "string",
                "description": "Unique track identifier, e.g. 'opening', 'analytical-1', 'tension-2'."
              },
              "file": {
                "type": "string",
                "description": "Path to audio file relative to episode data directory."
              },
              "startSec": {
                "type": "number",
                "description": "Start time in seconds from episode start."
              },
              "endSec": {
                "type": "number",
                "description": "End time in seconds from episode start."
              },
              "fadeInSec": {
                "type": "number",
                "default": 2,
                "description": "Fade-in duration in seconds."
              },
              "fadeOutSec": {
                "type": "number",
                "default": 3,
                "description": "Fade-out duration in seconds."
              },
              "volume": {
                "type": "number",
                "minimum": 0,
                "maximum": 1,
                "description": "Base volume (0-1). Typically 0.10-0.15 under narration, 0.20-0.30 during visual-only moments."
              },
              "mood": {
                "type": "string",
                "enum": ["contemplative", "analytical", "tension", "resolution", "neutral"],
                "description": "Mood category for sourcing reference. See AUDIO_DESIGN.md for mood → tempo/key mapping."
              },
              "beat": {
                "type": "string",
                "description": "Which narrative beat this track covers, e.g. 'opening', 'beat1', 'closing'."
              }
            }
          }
        }
      }
    },
    "segments": {
      "type": "array",
      "description": "Ordered list of visual segments. Segments may overlap (e.g. a TEMPLATE over FOOTAGE). Z-order convention: all background-layer segments render first (z=0), then all foreground-layer segments render on top (z=1). Within the same layer, later segments in the array render on top of earlier ones. HOLD segments sustain the previous visual. In FullEpisode.tsx, background segments are rendered as the first set of <Sequence> blocks, foreground as the second set.",
      "items": {
        "$ref": "#/$defs/segment"
      }
    }
  },
  "$defs": {
    "segment": {
      "type": "object",
      "required": ["id", "type", "startSec", "endSec", "layer"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique segment identifier, e.g. 'beat1-p1-footage-tsmc' or 'beat2-template-timeline'."
        },
        "type": {
          "type": "string",
          "enum": ["FOOTAGE", "IMAGE", "TEMPLATE", "TRANSITION", "HOLD"],
          "description": "FOOTAGE = stock video, IMAGE = still photo/archival, TEMPLATE = Remotion motion graphic, TRANSITION = beat title card (TitleTransition), HOLD = sustain previous visual (e.g. dramatic pause on stat)."
        },
        "startSec": {
          "type": "number",
          "description": "Start time in seconds from episode start."
        },
        "endSec": {
          "type": "number",
          "description": "End time in seconds from episode start."
        },
        "layer": {
          "type": "string",
          "enum": ["background", "foreground"],
          "description": "'background' = full-bleed behind narration (footage, images). 'foreground' = motion graphic overlay (templates, transitions). Multiple background segments at the same time = last one wins. Foreground renders on top."
        },
        "beat": {
          "type": "string",
          "description": "Which beat this segment belongs to, e.g. 'beat1', 'beat2'."
        },
        "priority": {
          "type": "string",
          "enum": ["P1", "P2", "P3"],
          "description": "Production priority from the script. P1 = essential, P2 = important, P3 = nice-to-have."
        },
        "narrationRef": {
          "type": "string",
          "description": "The narration paragraph this visual accompanies, for alignment debugging. First ~80 chars of the narration text."
        },

        "asset": {
          "type": "object",
          "description": "For FOOTAGE and IMAGE segments. References sourced media.",
          "additionalProperties": false,
          "properties": {
            "shotListId": {
              "type": "string",
              "description": "ID from shot-list.json, e.g. 'beat1-tsmc-aerial'."
            },
            "searchTerms": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Fallback search terms from the script, in priority order."
            },
            "source": {
              "type": ["string", "null"],
              "description": "Expected source: 'pexels', 'pixabay', 'unsplash', 'wikimedia', 'archive'. Null until asset sourcing has selected one."
            },
            "file": {
              "type": ["string", "null"],
              "description": "Resolved file path after sourcing. Null until asset is sourced."
            },
            "status": {
              "type": "string",
              "enum": ["pending", "resolved", "failed"],
              "default": "pending",
              "description": "Asset resolution status. 'pending' = not yet sourced, 'resolved' = file available, 'failed' = sourcing attempted but unsuccessful."
            },
            "placeholder": {
              "type": "string",
              "enum": ["solid-color", "last-frame"],
              "default": "solid-color",
              "description": "Fallback rendering when asset is pending/failed. 'solid-color' = branded dark background with segment ID label. 'last-frame' = sustain the last rendered frame."
            },
            "fallbackColor": {
              "type": "string",
              "description": "Hex color for solid-color placeholder. Defaults to dark.bg.base (#0D0D1A)."
            }
          }
        },
        "treatment": {
          "type": "object",
          "description": "Brand treatment for FOOTAGE and IMAGE segments.",
          "additionalProperties": false,
          "properties": {
            "ramp": {
              "type": "string",
              "enum": ["standard", "conflict", "editorial"],
              "default": "standard"
            },
            "composite": {
              "type": "string",
              "enum": ["background", "inset", "antipode", "raw"],
              "default": "background"
            },
            "opacity": {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "Override opacity. If omitted, uses composite mode default from BrandImage."
            }
          }
        },
        "template": {
          "type": "object",
          "description": "For TEMPLATE and TRANSITION segments. References a Remotion composition.",
          "additionalProperties": false,
          "properties": {
            "component": {
              "type": "string",
              "enum": [
                "TitleTransition",
                "KineticTypography",
                "DataChart",
                "TimelineComparison",
                "FrameworkDiagram",
                "ChoroplethMap",
                "RouteAnimation",
                "DecisionTree",
                "SplitComposition",
                "ProbabilityGauge",
                "ImageComposite",
                "PhotoMontage",
                "NetworkDiagram",
                "TimeSeriesChart",
                "SankeyFlow",
                "GameBoard",
                "BayesianUpdate",
                "StatReveal",
                "RadarChart",
                "AnnotatedImage",
                "EscalationLadder",
                "HorizontalTimeline",
                "BifurcationRoute",
                "DualTimeline",
                "DuelingFrameworks",
                "StrategicLandscape",
                "TimelineMorph"
              ],
              "description": "Remotion template component name."
            },
            "dataFile": {
              "type": "string",
              "description": "JSON data file path relative to data/episodes/<slug>/, e.g. 'kinetic-92-yield.json'."
            },
            "backdropId": {
              "type": ["string", "null"],
              "description": "Optional atmospheric PNG under this Remotion template only (Register 1). Must match an id from BACKDROP_MANIFEST in EditorialSurface.tsx (e.g. cartographic, reading-room). Omitted or null = solid paper from EditorialSurface. Ignored for TitleTransition and layered foreground segments in FullEpisode."
            }
          }
        },
        "hold": {
          "type": "object",
          "description": "For HOLD segments. Sustains the previous visual.",
          "additionalProperties": false,
          "properties": {
            "freeze": {
              "type": "boolean",
              "default": true,
              "description": "If true, freeze the last frame. If false, let video/animation continue playing."
            }
          }
        },
        "transition": {
          "type": "object",
          "description": "Transition behavior at segment boundaries.",
          "additionalProperties": false,
          "properties": {
            "in": {
              "type": "string",
              "enum": ["cut", "fade", "dissolve", "wipe-left", "wipe-right", "wipe-up", "blur-through", "color-wash", "iris", "match-cut", "whip-pan", "spatial-zoom"],
              "default": "cut"
            },
            "out": {
              "type": "string",
              "enum": ["cut", "fade", "dissolve", "wipe-left", "wipe-right", "wipe-up", "blur-through", "color-wash", "iris", "match-cut", "whip-pan", "spatial-zoom"],
              "default": "cut"
            },
            "durationSec": {
              "type": "number",
              "default": 0.5,
              "description": "Transition duration in seconds. Ignored for cut transitions."
            },
            "washColor": {
              "type": "string",
              "description": "Hex color for color-wash transitions, e.g. '#1C1814'."
            }
          }
        },
        "layered": {
          "type": "object",
          "description": "Layered composition config — composites this foreground segment over a background segment. Implements the [LAYERED:] visual mode.",
          "additionalProperties": false,
          "properties": {
            "backgroundSegmentId": {
              "type": "string",
              "description": "ID of the background segment to composite under this foreground."
            },
            "position": {
              "type": "string",
              "enum": ["full", "lower-third", "upper-third", "center-inset", "bottom-bar"],
              "default": "full",
              "description": "Spatial positioning of the foreground layer."
            },
            "blendMode": {
              "type": "string",
              "enum": ["normal", "multiply", "screen", "overlay", "soft-light"],
              "default": "normal"
            },
            "foregroundOpacity": {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "default": 0.95
            },
            "backgroundDim": {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "default": 0.2,
              "description": "Darken the background so foreground text pops. 0 = no dim."
            },
            "foregroundFadeInSec": {
              "type": "number",
              "default": 0.3
            },
            "vignette": {
              "type": "boolean",
              "default": false,
              "description": "Add a subtle vignette overlay for depth."
            }
          },
          "required": ["backgroundSegmentId"]
        },
        "notes": {
          "type": "string",
          "description": "Production notes from the script's italicized annotations."
        },
        "holdAfter": {
          "type": "number",
          "description": "Extra seconds to hold this segment after narration ends. Emitted from DIR: hold annotations in the script."
        },
        "holdBehavior": {
          "type": "string",
          "enum": ["freeze", "loop", "continue", "linger", "breathe", "land"],
          "default": "freeze",
          "description": "What to do during the holdAfter period."
        },
        "paceProfile": {
          "type": "string",
          "enum": ["urgent", "analytical", "breathing"],
          "description": "PACE: annotation applied to this segment, controlling duration multiplier."
        },
        "syncWords": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Key words from narration used for Whisper sync alignment in precise mode."
        },
        "soundCue": {
          "type": "object",
          "description": "Primary sound design marker for NLE editor (Layer 2 — transition SFX). Specifies which sound event should accompany this segment's entrance or key moment.",
          "additionalProperties": false,
          "properties": {
            "type": {
              "type": "string",
              "enum": ["beat-transition", "stat-reveal", "tension-rise", "tension-resolve", "map-whoosh", "quote-bell", "section-open", "end-stinger"],
              "description": "Sound event type. See AUDIO_DESIGN.md for the full palette."
            },
            "offsetSec": {
              "type": "number",
              "default": 0,
              "description": "Offset from segment start in seconds. 0 = plays at segment entrance."
            },
            "intensity": {
              "type": "string",
              "enum": ["subtle", "normal", "dramatic"],
              "default": "normal",
              "description": "Mix level. 'subtle' = barely audible texture, 'dramatic' = momentary foreground."
            }
          }
        },
        "renderQuality": {
          "type": "object",
          "description": "Optional per-segment render-quality override. Currently ADVISORY at the segment level — FullEpisode renders as a single Remotion composition and Remotion CLI flags apply at the composition level, not per-segment. To get per-clip render overrides today, use the `_render` block on individual data files instead (honored by render-episode.mjs and render-lambda.mjs). This segment-level field documents intent for the day FullEpisode learns true per-segment rendering (e.g. via separate composition-per-segment + ffmpeg concat).",
          "additionalProperties": false,
          "properties": {
            "crf": {
              "type": "integer",
              "minimum": 14,
              "maximum": 28,
              "description": "Constant Rate Factor for H.264. Lower = higher quality (and bigger file). Defaults to Remotion's 18; hero clips might use 14-16."
            },
            "imageFormat": {
              "type": "string",
              "enum": ["png", "jpeg"],
              "description": "Per-clip override for intermediate frame format. Defaults to project-wide setting in remotion.config.ts (currently png)."
            },
            "motionBlur": {
              "type": "boolean",
              "default": false,
              "description": "Enable per-frame motion blur sampling. Costs render time linearly with sample count; reserve for camera-heavy clips (RouteAnimation, ChoroplethMap zoom)."
            },
            "scale": {
              "type": "number",
              "minimum": 1,
              "maximum": 4,
              "description": "Render this clip at N× resolution then downscale during encoding. Supersampling — useful for fine-line graphics (FrameworkDiagram, TimeSeriesChart). 1 = no supersample (default)."
            }
          }
        },
        "soundCueSecondary": {
          "type": "object",
          "description": "Secondary transition SFX for segments with two distinct audio moments (e.g. entrance + key moment). Same schema as soundCue.",
          "additionalProperties": false,
          "properties": {
            "type": {
              "type": "string",
              "enum": ["beat-transition", "stat-reveal", "tension-rise", "tension-resolve", "map-whoosh", "quote-bell", "section-open", "end-stinger"]
            },
            "offsetSec": {
              "type": "number",
              "default": 0
            },
            "intensity": {
              "type": "string",
              "enum": ["subtle", "normal", "dramatic"],
              "default": "normal"
            }
          }
        },
        "textureCues": {
          "type": "array",
          "description": "Micro-SFX markers for template animation events (Layer 3 — texture hits). Each cue fires at a specific offset from segment start. Max 3 hits per 10-second window.",
          "items": {
            "type": "object",
            "required": ["type", "offsetSec"],
            "additionalProperties": false,
            "properties": {
              "type": {
                "type": "string",
                "enum": ["dot-click", "card-settle", "line-draw", "region-glow", "bar-grow", "node-pop", "page-turn"],
                "description": "Texture hit type. See AUDIO_DESIGN.md for descriptions."
              },
              "offsetSec": {
                "type": "number",
                "description": "Offset from segment start in seconds. Texture hits should be timed to match template animation events."
              },
              "volume": {
                "type": "number",
                "minimum": 0,
                "maximum": 0.2,
                "default": 0.08,
                "description": "Volume (0-0.2). Default 0.08. Texture hits should be 15-20dB below narration."
              },
              "label": {
                "type": "string",
                "description": "Human-readable label for cue sheet, e.g. 'first bar grows', 'Taiwan fills'. Optional."
              }
            }
          }
        }
      }
    }
  }
}
