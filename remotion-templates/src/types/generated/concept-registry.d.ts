/**
 * AUTO-GENERATED FILE — DO NOT EDIT.
 *
 * Generated by scripts/generate-schema-types.mjs from the matching JSON
 * Schema. Run `npm run gen:types` after editing any schema in data/*.
 *
 * Generator banner: v1 — 2026-05-18
 */
/**
 * Tracks every framework, foreign term, named concept, historical analogy, and strategic entity introduced across Parallax episodes. Enables cross-episode callbacks, visual consistency, and compounding knowledge.
 */
export interface ParallaxConceptRegistry {
  /**
   * Schema version (semver).
   */
  version: string;
  concepts: Concept[];
}
export interface Concept {
  /**
   * Lifecycle marker. 'draft' = the introducing episode hasn't published yet; this concept is provisional and tooling that surfaces concepts to viewers should skip it. 'published' (or absent — the default) = canonical and safe to consume.
   */
  _status?: "draft" | "published";
  /**
   * Kebab-case unique identifier (e.g., 'prisoners-dilemma', 'ka-bozi').
   */
  id: string;
  term: BilingualTerm;
  /**
   * Concept category. framework = analytical tool (game theory, Bayesian). foreign-term = non-English term with cultural weight (卡脖子). named-concept = a coined or established term (bifurcation, small yard high fence). historical-analogy = a specific historical episode used as a lens. entity = a company, organization, or regime relevant across episodes. mental-model = a reusable reasoning pattern (steelmanning, base rates). prediction = a falsifiable forward-looking claim with a timeframe (Oracle direction). person = a named individual whose ideas or actions appear across episodes (Nash, Schelling, Kennan). quote = a specific quoted statement worth tracking for reuse (term.en holds the quote text, attribution field names the source). source = a cited reference (paper, book, article, dataset) — sourceMeta carries the bibliographic detail.
   */
  type:
    | "framework"
    | "foreign-term"
    | "named-concept"
    | "historical-analogy"
    | "entity"
    | "mental-model"
    | "prediction"
    | "person"
    | "quote"
    | "source";
  /**
   * One-sentence definition as it would appear in a concept card. Written for the audience, not for internal reference.
   */
  definition: string;
  /**
   * The key non-obvious takeaway — what makes this concept worth introducing. The 'Wait, What?' moment.
   */
  insight?: string;
  introduced: Introduction;
  /**
   * Subsequent episodes that reference, deepen, or callback this concept.
   */
  appearances?: Appearance[];
  /**
   * IDs of concepts that are intellectually connected. Used for suggesting cross-references in future scripts.
   */
  relatedConcepts?: string[];
  /**
   * Which content pillar(s) this concept serves.
   */
  pillar?: ("historical-analogy" | "philosophical-framework" | "geopolitics")[];
  /**
   * Free-form tags for filtering (arc names, regions, disciplines).
   */
  tags?: string[];
  /**
   * Suggested visual treatment when this concept is referenced in a later episode (not re-introduced). E.g., '2s KineticTypography flash with original accent color' or 'brief FrameworkDiagram callback with dimmed detail'.
   */
  callbackVisual?: string;
  prediction?: Prediction;
  /**
   * For type='quote' entries: who said it (and when, if relevant). E.g., 'John Nash, 1950'. Used by lookup.py quote to surface the speaker alongside the quote text.
   */
  attribution?: string;
  /**
   * For type='source' entries: bibliographic detail used by lookup.py source and the source-sheet generator.
   */
  sourceMeta?: {
    author?: string;
    year?: number;
    publisher?: string;
    url?: string;
    /**
     * Page or section reference (e.g. 'p. 107' or 'ch. 4').
     */
    page?: string;
  };
}
export interface BilingualTerm {
  /**
   * English term or phrase.
   */
  en: string;
  /**
   * Chinese term (characters). Omit if not applicable.
   */
  cn?: string;
  /**
   * Pinyin romanization. Omit if not applicable.
   */
  pinyin?: string;
  /**
   * Abbreviated form for UI labels or lower-thirds (e.g., 'P.D.' for Prisoner's Dilemma).
   */
  short?: string;
}
/**
 * Where and how this concept was first introduced to the audience.
 */
export interface Introduction {
  /**
   * Episode ID (e.g., 'EP01').
   */
  episode: string;
  /**
   * Beat number where the concept is first introduced. Null when registered before its host episode is finalized (e.g. cross-episode frameworks).
   */
  beat: number | null;
  /**
   * Approximate timestamp range (e.g., '3:00-3:30'). Null until the episode publishes.
   */
  timestamp?: string | null;
  /**
   * Remotion template used for the introduction visual (e.g., 'KineticTypography', 'FrameworkDiagram'). Null when treatment is 'organic' (no dedicated visual).
   */
  template?: string | null;
  /**
   * cold-intro = dedicated visual moment to introduce. organic = woven into narration without a dedicated visual. callback = referencing a concept introduced in a prior episode.
   */
  treatment?: "cold-intro" | "organic" | "callback";
  /**
   * Hex color used for this concept's introduction (for visual consistency in callbacks). Null when treatment is 'organic' (no dedicated visual moment).
   */
  accentColor?: string | null;
}
export interface Appearance {
  /**
   * Episode ID.
   */
  episode: string;
  /**
   * Beat number where the concept appears. Null when the appearance isn't pinned to a specific beat (e.g. running motif).
   */
  beat?: number | null;
  /**
   * callback = brief visual reference to prior intro. deepened = concept is explored further with new facets. central = concept is the main subject of this episode. passing = mentioned in narration without visual treatment.
   */
  role: "callback" | "deepened" | "central" | "passing";
  /**
   * Template used (if any visual treatment). Null when role is 'passing' or callback uses no dedicated template.
   */
  template?: string | null;
  /**
   * What new angle or context this appearance adds.
   */
  notes?: string;
}
/**
 * Prediction-specific fields. Required when type = 'prediction', omit for other types.
 */
export interface Prediction {
  /**
   * The falsifiable prediction in plain language. E.g., 'China will not achieve sub-7nm chip production at scale by Q4 2027.'
   */
  claim: string;
  /**
   * Rough probability estimate at time of publication (0-1). E.g., 0.6 for 60%.
   */
  probability: number;
  /**
   * When to check this prediction. ISO date or quarter (e.g., '2027-Q4', '2028-06-30').
   */
  timeframe: string;
  /**
   * What specific observable event would prove this prediction wrong.
   */
  falsification: string;
  /**
   * The strongest evidence against the thesis that generated this prediction.
   */
  whatWouldChangeMyMind?: string;
  /**
   * Current status. open = not yet checkable. confirmed/falsified = resolved by evidence. partially-confirmed = directionally right but details differ. expired = timeframe passed without clear resolution. revised = probability updated in a later episode.
   */
  status: "open" | "confirmed" | "falsified" | "partially-confirmed" | "expired" | "revised";
  resolution?: {
    /**
     * Resolution outcome. confirmed/falsified = clear evidence. partially-confirmed = directionally right but details differ. expired = timeframe passed without clear resolution.
     */
    status?: "confirmed" | "falsified" | "partially-confirmed" | "expired";
    /**
     * When the prediction was resolved (ISO date).
     */
    date?: string;
    /**
     * What evidence confirmed or falsified the prediction.
     */
    evidence?: string;
    /**
     * Episode where the resolution was discussed (e.g., 'EP12' or a 'Was I Right?' special).
     */
    episode?: string;
    /**
     * Revised probability if the prediction was updated rather than resolved.
     */
    updatedProbability?: number;
  };
  /**
   * If this prediction is part of a named scenario, the scenario name (e.g., 'The Murano Scenario').
   */
  scenarioName?: string;
  /**
   * Specific observables the audience can track. E.g., 'ASML Q3 earnings — China revenue share', 'Kalshi contract on Taiwan strait conflict'.
   */
  watchSignals?: string[];
}
